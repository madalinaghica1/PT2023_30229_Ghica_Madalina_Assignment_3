package start;

import java.sql.SQLException;
import java.util.logging.Level;
import java.util.logging.Logger;

import BusinessLogic.ClientBLL;
import Model.Client;
import BusinessLogic.ProductBLL;
import Model.Product;
import BusinessLogic.OrderBLL;
import Model.Order;

//public class Start {
    /*protected static final Logger LOGGER = Logger.getLogger(Start.class.getName());

    public static void main(String[] args) throws SQLException {

    /*    Client client = new Client(6,"dummy name", "dummy address", "dummy@address.co", 12);

        ClientBLL clientBll = new  ClientBLL();
        int clientID = clientBll.insertClient(client);
        if (clientID > 0) {
            clientBll.findClientById(clientID);
        }


        // Generate error
        try {
            clientBll.findClientById(1);
        } catch (Exception ex) {
            LOGGER.log(Level.INFO, ex.getMessage());
        }


        //obtain field-value pairs for object through reflection
        reflectionExample.retrieveProperties(client);


    Product product = new Product(5,"apa", 30,  2);

    ProductBLL prodBll = new  ProductBLL();
    int pID = prodBll.insertProduct(product);
        if (pID>0) {
        prodBll.findProductById(pID);
    }


    // Generate error
        try {
        prodBll.findProductById(1);
    } catch (Exception ex) {
        LOGGER.log(Level.INFO, ex.getMessage());
    }


    //obtain field-value pairs for object through reflection
        reflectionExample.retrieveProperties(product);
*/
       /* Order order = new Order(1,"Mada", "apa",  2);

        OrderBLL ordBll = new  OrderBLL();
        int oID = ordBll.insertProduct(order);
        if (oID>0) {
            ordBll.findOrderById(oID);
        }


        // Generate error
        try {
            ordBll.findOrderById(1);
        } catch (Exception ex) {
            LOGGER.log(Level.INFO, ex.getMessage());
        }


        //obtain field-value pairs for object through reflection
        reflectionExample.retrieveProperties(order);
    }

}*/
